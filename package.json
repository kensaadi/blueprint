{
  "name": "blueprint",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Blueprint — declarative UI contract runtime + Builder. Interpreter + bridge for server-driven UI composition.",
  "license": "AGPL-3.0-only",
  "packageManager": "pnpm@10.7.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "dev": "turbo dev"
  },
  "devDependencies": {
    "turbo": "2.10.6",
    "typescript": "~6.0.2",
    "@types/node": "^24.12.3"
  }
}
