/**
 * @dashforge/blueprint-builder (sandbox preview)
 *
 * Visual editor for Blueprint contracts. NOT YET STARTED.
 *
 * Will become `@dashforge/blueprint-builder` when the runtime is stable
 * enough to support live preview inside the editor.
 *
 * Roadmap (V2 — after runtime V1 ships):
 *   - Drag-and-drop palette of native nodes + custom nodes
 *   - propsSchema-driven inspector panel
 *   - Live preview using `<DashBlueprint />`
 *   - Export contract as JSON for transport to backend
 */
export const __builder_placeholder = true;
